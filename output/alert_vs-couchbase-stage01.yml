groups:
- name: cpu_alerts
  rules:
  - alert: HighCpuUsageOnVsCouchbaseStage01
    expr: (100 - avg by(instance)(irate(node_cpu_seconds_total{mode="idle", host="vs-couchbase-stage01"}[5m]))
      * 100) > 80
    for: 5m
    labels:
      severity: warning
      host: vs-couchbase-stage01
      environment: 'null'
    annotations:
      summary: High CPU usage on vs-couchbase-stage01 ({{ $labels.instance }})
      description: CPU usage is over 80% on host vs-couchbase-stage01 ({{ $value }}%)
