groups:
- name: cpu_alerts
  rules:
  - alert: HighCpuUsageOnVsDev04
    expr: (100 - avg by(instance)(irate(node_cpu_seconds_total{mode="idle", host="vs_dev04"}[5m]))
      * 100) > 80
    for: 5m
    labels:
      severity: warning
      host: vs_dev04
      environment: 'null'
    annotations:
      summary: High CPU usage on vs_dev04 ({{ $labels.instance }})
      description: CPU usage is over 80% on host vs_dev04 ({{ $value }}%)
