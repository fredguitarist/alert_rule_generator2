groups:
- name: cpu_alerts
  rules:
  - alert: HighCpuUsageOnVsDbStage01
    expr: (100 - avg by(instance)(irate(node_cpu_seconds_total{mode="idle", host="vs_db_stage01"}[5m]))
      * 100) > 80
    for: 5m
    labels:
      severity: warning
      host: vs_db_stage01
      environment: 'null'
    annotations:
      summary: High CPU usage on vs_db_stage01 ({{ $labels.instance }})
      description: CPU usage is over 80% on host vs_db_stage01 ({{ $value }}%)
